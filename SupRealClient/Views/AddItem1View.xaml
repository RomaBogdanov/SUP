<Window x:Class="SupRealClient.AddItem1View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SupRealClient"
        mc:Ignorable="d"
        Title="Добавление" Height="150" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Label Grid.ColumnSpan="2" Content="Введите документ:"></Label>
        <TextBox Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding Path=Field, Mode=TwoWay}"></TextBox>
        <Button Grid.Row="2" Grid.Column="0" Content="ОК" Command="{Binding Ok}"></Button>
        <Button Grid.Row="2" Grid.Column="1" Content="Отмена" Command="{Binding Cancel}"></Button>
    </Grid>
</Window>
